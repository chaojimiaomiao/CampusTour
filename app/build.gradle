apply plugin: 'com.android.application'

android {
    signingConfigs {
        maomao {
            keyAlias 'maomao'
            keyPassword 'maomao0417'
            storeFile file('/Users/zhaibingjie/workspace/bingjie.jks')
            storePassword 'maomao'
        }
    }
    compileSdkVersion 23
    buildToolsVersion "24.0.2"

    sourceSets {
        main {
            jniLibs.srcDir 'libs'
        }
    }

    defaultConfig {
        applicationId "com.example.zhaibingjie.myapplication"
        minSdkVersion 15
        targetSdkVersion 23
        versionCode 1
        versionName "1.0"
    }
    buildTypes {
        release {
            minifyEnabled false
            signingConfig signingConfigs.maomao
        }
    }
}

dependencies {
    compile fileTree(include: ['*.jar'], dir: 'libs')
    androidTestCompile('com.android.support.test.espresso:espresso-core:2.2.2', {
        exclude group: 'com.android.support', module: 'support-annotations'
    })
    compile 'com.android.support:appcompat-v7:23.4.0'
    testCompile 'junit:junit:4.12'
    compile files('libs/baidumapapi_base_v4_1_1.jar')
}
